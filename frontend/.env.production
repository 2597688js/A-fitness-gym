# Backend API URL for production (Vercel deployment)
# This will be the same domain as your frontend (Vercel auto-handles /api routing)
# Or set to a custom domain if deployed separately
VITE_API_URL=https://your-domain.vercel.app
